/** Main login DTO. */
export interface LoginModel {
    username: string;
    password: string;
}

/** Main signup DTO. */
export interface SignupModel {
    firstName: string;
    surname: string;
    email: string;
    username: string;
    password: string;
}
